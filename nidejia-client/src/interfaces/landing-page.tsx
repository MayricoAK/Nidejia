export interface DealsProps {
  image?: string;
  title: string;
  slug: string;
  price: number;
  wide: number;
  capacity: number;
  wifi: number;
}

export interface CategoriesProps {
  icon: string;
  title: string;
  subtitle: string;
}
